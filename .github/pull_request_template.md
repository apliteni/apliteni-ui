<!-- Thanks for contributing to apliteni-ui! Keep the summary short and specific. -->

## What & why


## Verification
- [ ] `npm test` passes (unit + axe a11y)
- [ ] `node site/build.mjs` / `npm run build-storybook` build cleanly
- [ ] **No protected data** — no real customer/financial data, no personal emails or
      phone numbers, no internal infra IDs (Lessly service/org/product UUIDs,
      `*.lessly.run` hosts, ttl.sh image tags, deploy tokens) in code, fixtures,
      issues, or PR text. Demo data uses obvious placeholders (e.g. `Ada Lovelace /
      ada@apliteni.com`). See CONTRIBUTING → Data handling.

## Linked issue

Closes #
