# NNNN. The decision, as a sentence

- **Date:** YYYY-MM-DD
- **Status:** accepted · superseded by [NNNN](NNNN-name.md)
- **Code:** `path/to/file.js`, `path/to/other.js`

## What we ran into

The behaviour that forced a decision. Concrete: the input, what happened, what was expected.
Somebody who has never seen this should be able to reproduce it from this paragraph.

## What we decided

The rule, stated so it can be checked against the code. If there is a number in it, give the
number.

## Why not the alternatives

The options that were real, and what each would have cost. An option nobody considered is not an
alternative — leave it out.

## What this does not cover

The cases the decision knowingly leaves open, and what happens in them. This is the section that
stops the record being read as a guarantee it never made.
